testCaseId: "user_1_retrieve"
baseUri: "{{include baseUri}}"
auth:
    type: "{{include auth_info_1.type}}"
    username: "{{include auth_info_1.username}}"
    password: "{{include auth_info_1.password}}"
given:
  headers:
    Content-Type: "{{include content_type}}"
  pathParam:
    userId: 1
  log: true

when:
  method: GET
  path: "/api/users/:userId"

then:
  statusCode: 200
  headers:
    Content-Type: "application/json"
  cookie: {}
  body: {}

datacapture:
  var_dept_id: ""

log: "all"
delay: 2000
priority: 1
next: ""
