testCaseName: "GetCourse"
baseUri: {{include baseUri_new}}
auth: {{include auth_info_1_new}}

request:
  headers:
    Content-Type: "application/json"
  log: "all_newwwwww"
  method: "GET"
  path: "api/users?category={{param userId}}"
  body:
    {
      "userId": {{use GetStudent.userId}},
      "hometown": {{use GetStudent.hometown}}
    }

response:
  statusCode: 200
  headers:
    Content-Type: "application/json"
  cookie: {}
  body: {}

